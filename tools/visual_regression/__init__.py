"""Visual-regression harness for the UI Kit showcase (BL-047).

Headless-Chromium screenshots of the canonical templates in
`services/portal/ui-kit/pages/templates/`, pixel-diff'd against committed
baselines in `tests/visual/baselines/`. Drives both `pytest tests/visual/`
and `make visual-test`.
"""
