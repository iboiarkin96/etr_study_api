# Loaded after .env when APP_ENV=prod
LOG_LEVEL=INFO
# ADR 0037: PostgreSQL. Real credentials come from the deploy environment
# (secrets manager / ENV_FILE), never from a checked-in file. The value below
# is a placeholder that will fail startup validation if it reaches prod unchanged.
DATABASE_URL=postgresql+psycopg://REPLACE_ME:REPLACE_ME@REPLACE_ME:5432/study_app
